# tinyexec ships releases quickly during active triage. The version
# matrix in this MRE needs access to the latest published versions
# without waiting for the 7-day cooldown.
minimum-release-age-exclude[]=tinyexec
minimum-release-age-exclude[]=lint-staged
